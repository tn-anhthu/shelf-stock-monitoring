# Copy this file to `.env` and fill in your real key — never commit `.env` itself
# (it's already in .gitignore). Used for the Task I LLM-escalation experiment
# (classify_crop tie-breaker via Claude vision API) discussed in this session.

# Anthropic API key — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Model to use for the escalation experiment — Haiku is cheapest and has vision,
# no need for Sonnet/Opus for a simple "pick 1 of 2-3 SKUs" classification prompt.
CLAUDE_ESCALATION_MODEL=claude-haiku-4-5-20251001

# === Google Gemini (pilot, tuỳ chọn) ===
GEMINI_API_KEY=your-gemini-api-key
# gemini-2.5-flash (and 2.5-flash-lite) are no longer available to new API
# keys as of 2026-07 ("no longer available to new users") -- verified live
# against this project's own key. gemini-3.6-flash confirmed working (a full
# flash-tier model, not lite/preview) at $1.50/$7.50 per M input/output
# tokens (https://ai.google.dev/gemini-api/docs/pricing, checked 2026-07-27).
GEMINI_ESCALATION_MODEL=gemini-3.6-flash
LLM_PROVIDER=anthropic  # anthropic | gemini
